Android app built along side the teamTreehouse android track.

Some updates include putting all strings in /res/.  Implemented text string sin an array + broke out a bunch of classes during refactoring.
